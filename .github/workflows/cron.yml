name: Scheduled Shorts Pipeline

on:
  schedule:
    - cron: '0 1 * * *'   # Batch 1: 01:00 UTC (9 PM US Eastern)
    - cron: '0 11 * * *'  # Batch 2: 11:00 UTC (7 AM US Eastern)
    - cron: '0 18 * * *'  # Batch 3: 18:00 UTC (2 PM US Eastern)
  workflow_dispatch:      # Allows manual trigger from the GitHub UI
    inputs:
      pipeline_type:
        description: 'Pipeline Type to run'
        required: true
        default: 'shorts'
        type: choice
        options:
          - shorts
          - regular

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Create .env file from secrets
        run: |
          cat << 'EOF' > .env
          ${{ secrets.ENV_FILE_CONTENT }}
          EOF

      - name: Create YouTube Auth Files
        run: |
          mkdir -p config backend/config
          cat << 'EOF' > config/client_secrets.json
          ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
          EOF
          cat << 'EOF' > config/youtube_token.json
          ${{ secrets.YOUTUBE_TOKEN }}
          EOF
          cp config/client_secrets.json backend/config/client_secrets.json
          cp config/youtube_token.json backend/config/youtube_token.json

      - name: Start Docker Compose Services
        run: |
          # Start postgres, redis, api, and celery worker
          docker compose up --build -d postgres redis api worker
          
      - name: Wait for API to be ready
        run: |
          echo "Waiting for API to start..."
          count=0
          max_attempts=30
          until curl -s -f http://localhost:8000/health; do
            count=$((count + 1))
            if [ $count -ge $max_attempts ]; then
              echo "❌ API failed to start after $max_attempts attempts!"
              echo "=== API Container Logs ==="
              docker compose logs api
              echo "=== Postgres Logs ==="
              docker compose logs postgres
              exit 1
            fi
            echo "Still waiting... ($count/$max_attempts)"
            sleep 5
          done
          echo "API is healthy!"

      - name: Run Pipeline Script
        run: |
          # Run the script inside the 'worker' container with selected pipeline type
          docker compose exec -T worker python scripts/trigger_and_wait.py --type "${{ github.event.inputs.pipeline_type || 'shorts' }}"


      - name: Stop Docker Services
        if: always()
        run: |
          docker compose down

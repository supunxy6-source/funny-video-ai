# YouTube OAuth Credentials Directory

Place your Google Cloud Console OAuth 2.0 Client Secret JSON file in this directory.

Rename the downloaded JSON file to:
`client_secrets.json`

File path:
`config/client_secrets.json`
